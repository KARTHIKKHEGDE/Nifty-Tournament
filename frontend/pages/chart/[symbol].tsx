// This route handles /chart/[symbol] URLs (Zerodha-style)
// It renders the same Dashboard component to keep UI consistent
import DashboardHome from '../dashboard/index';

export default DashboardHome;
